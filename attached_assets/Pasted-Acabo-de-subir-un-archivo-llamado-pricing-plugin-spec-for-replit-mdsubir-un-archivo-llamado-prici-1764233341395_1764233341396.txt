Acabo de subir un archivo llamado *pricing_plugin_spec_for_replit.mdsubir un archivo llamado **pricing_plugin_spec_for_replit.md*.

Por favor, léelo completamente e implementa todo el proyecto como un *módulo plug-in no invasivo*, exactamente como se describe.

Reglas importantes que debes seguir:

1. *No modifiques ni elimines ninguna lógica, archivo, ruta, modelo, tabla o regla de negocio existente en el programa actual.*
2. Todo el código nuevo debe estar completamente aislado dentro de un nuevo módulo/carpeta llamado */pricing_plugin*.
3. El plug-in solo debe funcionar cuando la bandera *ENABLE_PRICING_PLUGIN = true* esté activada. Si es false, no debe cargarse nada ni ejecutarse nada.
4. Todas las nuevas rutas de API del backend deben estar dentro del namespace:
   */api/pricing-plugin/*
   para evitar colisiones con el sistema actual.
5. La integración con la base de datos debe ser *solo aditiva*.
   Puedes agregar tablas nuevas según lo especificado, pero jamás modificar estructuras existentes.
6. La lógica de precios debe implementarse exactamente como se define en el documento.
7. Debes crear:

   * Rutas backend para simulación, configuración admin, suscripciones e invoices
   * Un job seguro de facturación recurrente, aislado en el plug-in
   * Una página frontend para el simulador de precios del cliente
   * Una página frontend para la configuración de precios del administrador
     Todo debe quedar aislado y opcional, sin interferir con el sistema actual.

Tus tareas:

1. Crear todo el backend del plug-in (Node.js o Python, según el stack que ya use este proyecto).
2. Implementar la lógica de precios como una función reutilizable.
3. Agregar las nuevas tablas de base de datos exactamente como se especifica.
4. Crear todos los endpoints de API definidos en el documento.
5. Crear las páginas frontend para:

   * el simulador de precios del cliente
   * la configuración de precios del admin
6. Implementar un job seguro e independiente de facturación recurrente.
7. Asegurar que todo el plug-in pueda activarse/desactivarse de forma segura mediante la feature flag.
8. Garantizar que probar este módulo *no pueda romper ni sobrescribir* ningún código o dato existente.

No improvises más allá de la especificación.
Sigue el documento subido *exactamente* y genera solo código aditivo, no destructivo.

Cuando estés listo, comienza a generar la estructura de archivos y la implementación.